<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flyhz.shop.persistence.dao.ConsigneeDao">
	<insert id="insertConsignee">
		insert into consignee(name,contury,city,province,town,address,zipcode,mobilephone,user_id) 
		values(#{name},#{contury},#{city},#{province},#{town},#{address},#{zipcode},#{mobilephone},#{userId})
	</insert>
	<select id="getModelById" parameterType="Integer" resultType="ConsigneeModel">
	    select id, name,contury,city,province,address,zipcode,mobilephone,user_id userId
	    from consignee
	    where id = #{id}
	</select>
	<select id="getModel" parameterType="ConsigneeModel" resultType="ConsigneeModel">
	    select id, name,contury,city,province,address,zipcode,mobilephone,user_id userId
	    from consignee
	    where id = #{id} and user_id = #{userId}
	</select>
	<select id="getConsigneesByUserId" parameterType="Integer" resultType="ConsigneeDto">
	    select id, name,contury,city,province,address,zipcode,mobilephone,user_id userId
	    from consignee
	    where user_id = #{id}
	</select>
</mapper>